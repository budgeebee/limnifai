version: '3.8'

services:
  whatever:
    build: .
    ports:
      - "1477:1477"
    volumes:
      - .:/app
      - /app/node_modules
    environment:
      - NODE_ENV=development
    command: npm run dev
