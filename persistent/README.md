# Persistent

This directory contains files that survive the nightly OpenCode burns.

## Contents

- `.env` — API keys (gitignored, never committed)

## Usage

Copy your `.env` file here. It will NOT be committed to git.
OpenCode never touches this directory.
